{
  "name": "netroach-desktop",
  "version": "0.2.4",
  "private": true,
  "description": "Desktop wrapper for the Netroach local dashboard.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
