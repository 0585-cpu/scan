# Netroach 0.2.0 Release Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans for implementation plans; this release records and verifies already implemented work in the existing checkout without creating or deleting worktrees.

**Goal:** Bump the completed SYN/dashboard changes to 0.2.0 and create the requested GitHub release.

**Architecture:** Keep the existing Rust engine, Python API/dashboard, and Tauri/NSIS packaging. Release the tested source commit and matching versioned artifacts without changing repository visibility or user data.

**Tech Stack:** Python, Rust, Tauri, NSIS, Playwright, GitHub CLI.

**Spec:** User request to bump the version and release on GitHub; `docs/release-checklist.md` and `docs/desktop-packaging.md` supply the existing gates.

## Global Constraints

- Target version: `0.2.0`; tag: `v0.2.0`.
- Preserve existing related changes; do not force push, overwrite old releases, or change repository visibility.
- Do not commit generated executables, Npcap installer bytes, databases, logs, or credentials.
- Npcap-containing assets require confirmation of personal-use storage in the private release; no public/external redistribution is authorized.
- Only controlled local fixtures may be scanned during validation. Clean-VM installation and remote SYN acceptance remain separate, unverified gates.

## Task 1: Version and release notes

**Files:** `netroach/version.py`, both product Cargo manifests and lockfiles, desktop JSON manifests/lockfile, `CHANGELOG.md`, `README.md`, `docs/install.md`, `docs/release-checklist.md`.

- [x] Read Git status, repository/release metadata, applicable instructions, and packaging documentation.
- [x] Replace only product version fields with `0.2.0`; preserve third-party dependency versions and historical evidence records.
- [x] Record SYN, evidence, dashboard, and regression changes in the changelog.
- [x] Parse all product metadata and assert the versions are identical; run `python -m netroach --version`.

## Task 2: Validate and package

**Files:** Existing application/tests; generated outputs stay under ignored `target/` directories.

- [x] Run `.venv\Scripts\python.exe -m ruff check .`, `-m mypy`, and `-m unittest discover -s tests`, with packaged Chromium available for browser tests. Python: 451 passed; mypy: 29 source files.
- [x] Run `cargo fmt --check`, `cargo test --locked -p netroach-engine`, and the same test command with `--features syn-sweep` and the local Npcap SDK library path. Default: 43 unit + 13 integration; SYN: 63 unit + 12 integration passed.
- [ ] Build the agreed installer with `.venv\Scripts\python.exe tools\build_desktop.py`; a confirmed personal build uses `--syn-sweep --npcap-sdk-lib C:\npcap-sdk\Lib\x64 --npcap-installer <explicit local official installer> --bundles nsis` as documented.
- [ ] Verify frozen runtime version, dashboard/source agreement, installer SHA-256 sidecar, and package prerequisites. Record unverified installed/clean-VM gates honestly.

## Task 3: Publish the verified revision

**Files:** Reviewed source/docs/tests only; release assets are the versioned installer and checksum.

- [ ] Inspect the final diff and explicit staging list, including untracked SYN implementation/tests. Exclude all private/generated payloads from Git.
- [ ] Commit as `release: prepare Netroach 0.2.0`, create annotated tag `v0.2.0`, and push the branch/tag without force, only if the remote has not diverged.
- [ ] Create a draft release at the exact tag, attach only agreed artifacts, compare uploaded size/digest to local files, and publish it. Preserve the existing v0.1.0 release.
- [ ] Confirm release URL, tag commit, version, assets, and workflow status; report any unfinished CI or acceptance gate.
